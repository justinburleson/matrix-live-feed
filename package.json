{
  "name": "matrix-live-feed",
  "version": "1.0.0",
  "description": "Matrix-style live data feed (SSE + ingest) for Make.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
